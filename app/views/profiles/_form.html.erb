<%= form_for(@profile) do |f| %>
  <% if @profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile.errors.count, "error") %> prohibited this profile from being saved:</h2>

      <ul>
      <% @profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="actions">
      <div class="form-group">
          <%= f.label :Job_Title %>
          <%= f.text_field :job_title, class: "form-control" %>
        </div>
         <div class="form-group">
          <%= f.label :What_are_your_hobbies? %>
          <%= f.text_field :hobbies, class: "form-control" %>
        </div>
         <div class="form-group">
          <%= f.label :What_skills_do_you_have? %>
          <%= f.text_field :skill, class: "form-control" %>
        </div>
         <div class="form-group">
          <%= f.label :What_is_your_favorite_food %>
          <%= f.text_field :food, class: "form-control" %>
      
     </div> 
    <%= f.submit class: "btn btn-primary" %>
  
  </div>
<% end %>
